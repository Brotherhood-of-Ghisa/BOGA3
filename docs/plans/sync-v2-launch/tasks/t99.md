# t99: complete the deviations log — add the three missing merged-PR entries

**Type:** build (audit remediation — documentation only; no source change)

**Problem:** The audit found `plan.md` `## Deviations log` is INCOMPLETE. The
protocol requires one line per merged PR; three merged PRs have no dedicated
entry. Root cause: the docs-consolidation PR (#134) that landed the final
`plan.md`/`status.md` on `main` was prepared in iteration 31 BEFORE #132 and #133
merged, and was never updated to capture them after they landed (#134 merged ~20s
after #132). The missing entries are:

- **#132 (tINVENTORY)** — a merged PLAN TASK. It appears in the DAG and task list
  but has no `## Deviations log` line. This is the material gap (a plan task with
  no merge-log entry).
- **#133** — out-of-band fix (`fix(sync): seed muscle groups before the
  round-trip cycle re-materialises muscle mappings`). Mentioned in passing inside
  #131's entry ("separate fix spawned; tracked") but has no dedicated line.
- **#130** — out-of-band docs (`docs(testing): mandatory per-change slow-gate
  triggers`). Mentioned inside #129's entry and in `status.md` iteration 31's
  out-of-band list, but has no dedicated line.

This is an audit-trail completeness gap only: every plan OUTCOME is delivered,
tested, and green on the integration branch (the audit reproduced quality-fast
765, `test:sync:reinstall-parity` 7/7, and `test:sync:infra` 6/6 on `main`
`e7f2638`). No code, test, or outcome is affected. The fix is purely to make the
merge history complete so the plan is auditable.

**Inputs:**
- `docs/plans/sync-v2-launch/plan.md` `## Deviations log` (current head).
- The three PRs' merge facts (all merged 2026-06-04):
  - #132 `[tINVENTORY] non-unit test inventory + hygiene review` — closure task;
    inventoried the non-unit (Maestro / Supabase / mock-device-DB) tests in
    `docs/testing/sync-v2-non-unit-test-inventory.md`; keep/drop + principles
    review; per `status.md` iter 31: 33 suites inventoried, 0 drops, 1 fix.
  - #133 `fix(sync): seed muscle groups before the round-trip cycle re-materialises
    muscle mappings` — FIXTURE-only fix to `cycle-round-trip.test.ts` (the suite
    mocked `@/src/data/bootstrap`, bypassing the boot-time muscle-group seed →
    FK violation under enforcement). Production code confirmed correct/unchanged;
    `test:sync:infra` 6/6 green, idempotent.
  - #130 `docs(testing): mandatory per-change slow-gate triggers` — testing-strategy
    spec edit (UX/device change → slow FE Maestro gates; infra change → slow BE
    Supabase gates); the per-change slow-gate-trigger policy the tGATE bug (#129)
    motivated.

**Outcomes:**
- `plan.md` `## Deviations log` has one line for EACH of #132, #133, #130, in the
  same one-line-per-PR format as the existing entries (`- <label> (PR #N, merged
  2026-06-04): <one-line summary>`), placed in merge order relative to the
  existing tail.
- No other change: source, tests, and the rest of the plan files are untouched
  (this is a documentation-completeness remediation, not a code change).

**Output artifact:**
- The edited `docs/plans/sync-v2-launch/plan.md` (three appended `## Deviations
  log` lines). No code/test/spec files change.

**Out of scope:**
- Any source/test/spec change. Every outcome is already delivered and green;
  this card only closes the audit-trail gap.
- Re-logging #134 itself (the docs-consolidation PR that landed the plan dir on
  `main` — it IS the log carrier and is conventionally exempt, like the prior
  plans' #104/#105 retirement PRs).
