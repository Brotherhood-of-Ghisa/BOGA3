# t4: `sys_*` → `seed_*` bundle slug rename

**Type:** build

**Problem:** The seed bundle uses `sys_*` IDs (e.g. `sys_barbell_bench_press`).
"system" implies an ownership tier the v2 server schema deleted (every row is
per-user from day one). The launch contract renames every bundle slug to the
`seed_*` lineage prefix so bundle rows carry stable, deterministic slug IDs
that bundle migrations, idempotent push retries, and cross-version stability all
key off. Since the v2 cut wiped the server and re-created the client entity
tables, there are no `sys_*` rows in flight to migrate — this is a one-time
bundle-data rename.

**Inputs:**
- t3 merged: the bootstrapper now owns when the seeder runs, so the renamed
  bundle is inserted via the bootstrapper's `rowsPulled == 0` branch. (See t3
  `## Output artifact`.)
- Binding design (authoritative — cite, do not restate):
  `docs/plans/sync-v2/designs/t3.md` §2.1 (slug ID shape:
  `exercise_definitions.id = seed_<snake_case_slug>`;
  `exercise_muscle_mappings.id = seed_map_<exercise>__<muscle>`), §2.3 (the
  rename rationale and the "no server rows to migrate" fact), §2.2 (why slugs).
- The bundle data + seeder:
  `apps/mobile/src/data/exercise-catalog-seeds.ts`
  (`SYSTEM_EXERCISE_DEFINITION_SEEDS`,
  `SYSTEM_EXERCISE_MUSCLE_MAPPING_SEEDS`, the seeder body and its validation /
  verification helpers).
- `muscle_groups.id` stays a bare slug (`chest`, `back_lats`) — client-only
  taxonomy, no prefix (design t3.md §2.1). Do NOT prefix muscle-group ids.
- This is a bundle-DATA change, not a schema migration — add NO Drizzle
  migration (carry-over: the `sys_*→seed_*` rename is bundle data).

**Outcomes:**
- Every `exercise_definitions` bundle slug uses the `seed_*` prefix; every
  `exercise_muscle_mappings` bundle slug uses the `seed_map_<exercise>__<muscle>`
  form. (Maps to plan outcome: `sys_*` → `seed_*` rename lands; stable slug IDs.)
- No `sys_*` identifier remains in the bundle module or any code/test that keys
  off bundle ids. `muscle_groups` ids remain bare slugs.
- The seeder's validation/verification still passes against the renamed bundle
  (counts and mapping coverage unchanged).
- No Drizzle migration is added.
- Jest covers: bundle ids match the `seed_*` / `seed_map_*__*` shape; mapping
  ids reference exercise slugs that exist in the definitions bundle; seeder
  verification passes.
- Quality gate `./scripts/quality-fast.sh frontend` passes.

**Output artifact:**
- Renamed slugs throughout `apps/mobile/src/data/exercise-catalog-seeds.ts`
  (and any fixture/test referencing the old `sys_*` ids).
- Jest spec asserting the slug shape under `apps/mobile/app/__tests__/`.

**Out of scope:**
- The bundle-migration runtime loop (t5).
- Adding or changing the set of seeded exercises (rename only — no content
  churn beyond the id prefix).
- Any server migration.
