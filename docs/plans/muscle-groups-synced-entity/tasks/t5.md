# t5: seeding + bootstrap + boot FK enforcement

**Type:** build

**Problem:** `muscle_groups` is currently seeded as a standalone every-boot additive taxonomy
(`seedMuscleGroups` via `seedBootDataLayer`) and is treated as never-synced by bootstrap and
account-wipe. As a synced entity it must instead be seeded `local_dirty = 1` through the same
version-marker-gated starter-catalog path as `exercise_definitions` (`seedSystemExerciseCatalog`),
removed from the boot-only seed, and wiped/recovered generically. Separately, FK enforcement
(`PRAGMA foreign_keys = ON`) must be enabled at boot — after migrations, before seeding — so the FK
is live in production; the pragma is a no-op inside a transaction and table-rebuild migrations need
it off, so it must be set post-`migrate()`.

**Inputs:** t4 merged (the registry now knows `muscle_groups`). Binding contract in
`designs/t1.md ## Decision` (item 1 — `muscle_groups` is now a dirty-seeded synced entity). Key
files:
- `apps/mobile/src/data/bootstrap.ts` — remove `muscle_groups` from `seedBootDataLayer` /
  `runMuscleGroupSeed` (the standalone every-boot path goes away); enable
  `PRAGMA foreign_keys = ON` after `runRuntimeMigrations()` and before any seed in
  `prepareLocalDataLayer` (~line 142); rewrite the now-false "client-only / never syncs" comments
  (~lines 54-79, 123-133).
- `apps/mobile/src/data/exercise-catalog-seeds.ts` — `seedSystemExerciseCatalog` (~line 4269)
  already seeds `muscle_groups` rows; make those rows ride the same dirty contract
  (`localDirty: true`, `localUpdatedAtMs: seedStampMs`) as the exercise definitions so they push;
  remove the standalone `seedMuscleGroups` export (~line 4219) if nothing else needs it, and update
  its callers. The seed stays gated by the `appliedSeedMigrationAppVersion` marker.
- `apps/mobile/src/sync/account-wipe.ts` — `muscle_groups` is now one of the per-user entity tables
  wiped in `wipeLocalTables` (it is a Layer 0 parent — delete in child-before-parent order); drop
  the "client-only" preservation note (~lines 60-62) and the comment claiming it must outlive an
  account switch.

**Outcomes (maps to plan outcomes 6, 8 partial):**
- Boot no longer seeds `muscle_groups` via a standalone additive seed; `seedBootDataLayer` /
  `runMuscleGroupSeed` no longer touch `muscle_groups` (and the dead standalone `seedMuscleGroups`
  path is removed).
- `seedSystemExerciseCatalog` seeds `muscle_groups` rows `local_dirty = 1` with a monotonic stamp,
  gated by `appliedSeedMigrationAppVersion`, exactly like `exercise_definitions`; the seed
  verification (if any) covers `muscle_groups`.
- `PRAGMA foreign_keys = ON` is set at boot after migrations and before seeding;
  the FK is enforced at runtime.
- `wipeLocalTables` clears `muscle_groups` and the next sign-in recovers it from the server via the
  generic pull path; no client-only special-casing remains in `account-wipe.ts`.
- The now-false "client-only / never syncs" comments in `bootstrap.ts` and `account-wipe.ts` are
  rewritten to the synced-entity reality (self-contained, no plan/card references).
- `./scripts/quality-fast.sh`, `npm run test:handles`, and `./scripts/quality-slow.sh backend` are
  green (this touches data bootstrap + sync). Evidence in the PR.

**Output artifact:** `apps/mobile/src/data/bootstrap.ts` (FK pragma enabled post-migrate;
`muscle_groups` removed from boot seed; comments fixed), `apps/mobile/src/data/exercise-catalog-seeds.ts`
(`muscle_groups` seeded dirty in `seedSystemExerciseCatalog`; standalone path removed),
`apps/mobile/src/sync/account-wipe.ts` (`muscle_groups` wiped generically; client-only note removed).

**Out of scope:** Server DDL/RPC (t2). Client schema / baseline (t3). Registry (t4). Drift checker
(t6). Test-harness FK default + coverage + comment fixes (t7) — except keeping the gates this card
runs green. Docs (t8). Any bespoke muscle-group seed/wipe/drift/version-marker handling.
