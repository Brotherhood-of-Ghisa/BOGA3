# t2: server baseline — app_public.muscle_groups + composite FK

**Type:** build

> Updated from t1: see designs/t1.md ## Decision

**Problem:** The server has no `app_public.muscle_groups` table, and
`exercise_muscle_mappings.muscle_group_id` is stored as opaque text with no FK. To make
`muscle_groups` a per-user synced entity with a legitimate Layer 0 → Layer 1 FK, the squashed server
baseline must gain the new table (mirroring `exercise_definitions`) and promote the mappings column
to a real composite FK. The remote will be reset, so this edits the clean-room baseline in place —
no incremental migration. The push and pull RPCs enumerate the entity tables explicitly (per-entity
upsert branches in push; a static `UNION ALL` + `case`-by-layer in pull), so `muscle_groups` must be
wired into both for it to round-trip.

**Inputs:** t1 merged; binding contract in `designs/t1.md ## Decision` (items 2, 3, 4 — the server
table DDL, the FK promotion, and the wire field map). Edit three files:
- `supabase/migrations/20260525120000_sync_v2_clean_room.sql`:
  - `app_public.exercise_definitions` block (~line 153) is the structural template.
  - The mappings block (~line 239) carries the in-file comment to fix and the table to amend.
  - RLS policies block (~line 493) and grants block (~line 578) are the patterns to extend.
  - Triggers block (~line 396) shows the `touch_server_received_at` / `owner_user_id_immutable`
    trigger pair to add for the new table.
- `supabase/migrations/20260525130000_sync_v2_push_rpc.sql`: the per-entity `IF/ELSIF` upsert
  dispatch (`exercise_definitions` branch ~line 177) is the template for a new `muscle_groups`
  upsert branch (LWW on `(owner_user_id, id)`, the wire columns from `designs/t1.md ## Decision`
  item 4).
- `supabase/migrations/20260525130100_sync_v2_pull_rpc.sql`: add `'muscle_groups'` to the Layer 0
  array in the `case v_layer` (~line 242) and add a `union all ... from app_public.muscle_groups`
  leg (`exercise_definitions` leg ~line 290 is the template).

**Outcomes (maps to plan outcomes 1, 2):**
- `app_public.muscle_groups` exists per `designs/t1.md ## Decision` item 2: composite PK
  `(owner_user_id, id)`; columns `display_name`, `family_name`, `sort_order`, `is_editable`,
  `created_at`, `updated_at`, `deleted_at`, plus the universal `client_updated_at_ms` /
  `server_received_at`; the `muscle_groups_owner_received_idx` index and the `family_name` /
  `sort_order` / `display_name` / `deleted_at` indexes; the two structural triggers; the four owner
  RLS policies; grants to `authenticated` and `service_role`; zero CHECK constraints.
- `exercise_muscle_mappings` gains the composite FK
  `(owner_user_id, muscle_group_id) → app_public.muscle_groups(owner_user_id, id)`
  `deferrable initially deferred` per `designs/t1.md ## Decision` item 3 (constraint name and
  on-delete action as pinned there).
- The in-file comment (~line 240) no longer claims `muscle_groups` is client-only / opaque text with
  no FK; it describes the per-user synced FK.
- `sync_push` upserts `muscle_groups` (new per-entity branch) and `sync_pull` returns
  `muscle_groups` rows in Layer 0, so the entity round-trips. The pull Layer 0 array and the
  `union all` cover it.
- The slow backend gate (`./scripts/quality-slow.sh backend`) is green: the local Postgres applies
  the edited baseline + RPCs cleanly and the existing sync-v2 contract suites pass. Evidence in the
  PR.

**Output artifact:**
- `supabase/migrations/20260525120000_sync_v2_clean_room.sql` containing the
  `app_public.muscle_groups` table + indexes + triggers + RLS + grants and the amended
  `exercise_muscle_mappings` composite FK + corrected comment.
- `supabase/migrations/20260525130000_sync_v2_push_rpc.sql` with the `muscle_groups` upsert branch.
- `supabase/migrations/20260525130100_sync_v2_pull_rpc.sql` with `muscle_groups` in the Layer 0
  array and a `union all` leg.

**Out of scope:** Any incremental/appended server migration (edit the squash + RPCs in place).
Client-side changes. The drift checker (t6). Any bespoke muscle-group logic beyond the generic
per-entity branch/leg pattern the other eight entities already use.
