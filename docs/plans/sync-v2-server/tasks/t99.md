# t99: merge orchestrator plan-tracking commits

**Type:** build

**Problem:** Audit of the sync-v2-server plan found three integration-branch
failures whose fix already exists on `origin/claude/pensive-hoover-44a409`
(the orchestrator's session branch) but never landed on `main`:

1. `docs/plans/sync-v2-server/plan.md` `## Deviations log` reads literally
   `<empty until first merge>` on `origin/main` HEAD `df5e97f`. The five
   expected entries (t1/t2/t3/t4/tFINAL) exist on the audit branch but were
   never propagated.
2. `docs/plans/sync-v2-server/status.md` does not exist on `origin/main`.
   The file with iterations 1 through 9 exists on the audit branch.
3. `docs/plans/sync-v2-server/plan.md` `## Outcomes` lines 74-77 still
   encode the original (incorrect) layer partition (`Layer 0: gyms,
   exercise_definitions, exercise_tag_definitions; Layer 1: sessions,
   exercise_muscle_mappings`). The actual implementation
   (`apps/mobile/src/sync/topo-order.ts`,
   `supabase/migrations/20260525130100_sync_v2_pull_rpc.sql` `case` mapping,
   `supabase/tests/sync-v2-pull-fk-closure.sh` partition assertion)
   uses the corrected partition (`Layer 0: gyms, exercise_definitions;
   Layer 1: sessions, exercise_muscle_mappings, exercise_tag_definitions`).
   The plan-on-`main` therefore contradicts what shipped and what the
   tFINAL test (outcome 8a) actually asserts.

All three issues are documentation-only. The code, migrations, RPCs, drift
checker, and tFINAL tests are correct and the quality gates
(`./scripts/quality-slow.sh backend`, `./scripts/quality-fast.sh`) both
exit 0 on `main` HEAD.

**Inputs:**

- `origin/main` HEAD `df5e97f` (post-tFINAL-merge).
- `origin/claude/pensive-hoover-44a409` containing the eight unmerged
  plan-tracking commits in range `origin/main..origin/claude/pensive-hoover-44a409`.
  Each commit only touches `docs/plans/sync-v2-server/plan.md` and/or
  `docs/plans/sync-v2-server/status.md`:
  - `0f7a79d` — sync-v2-server status + t1 deviation propagation
  - `18f7467` — sync-v2-server status iter 5 (stall recovery)
  - `2cd3e4e` — sync-v2-server outcomes + deviations for t2 topo correction
  - `21c28f2` — t4 card adopts the corrected layer partition (touches
    `tasks/t4.md` — verify this is still wanted; if t4 already shipped with
    the correction encoded only in code, this card update is doc-only)
  - `fc8be67` — sync-v2-server status iter 6 (all parallel PRs approved)
  - `6f3bca2` — sync-v2-server iter 7 — t2/t3 merged, deviations logged
  - `3de1f87` — sync-v2-server iter 8 — all build tasks merged; dispatching tFINAL
  - `29c58ed` — sync-v2-server iter 9 — all merged; dispatching audit
  - `bcc4e16` — propagate sync-v2-server build-wave deviations downstream
- Audit comment on PR #74:
  https://github.com/Brotherhood-of-Ghisa/BOGA3/pull/74#issuecomment-4548438249

**Outcomes:**

- The eight commits above are landed on `main` (cherry-pick or open a
  single squash PR titled `[docs] sync-v2-server — propagate plan
  tracking + deviations`). After merge, on `main` HEAD:
  - `docs/plans/sync-v2-server/plan.md` `## Deviations log` carries the
    five entries one per merged PR (t1/#69, t2/#72, t3/#71, t4/#73,
    tFINAL/#74), each ≥ one line summarising what shipped + any deviation.
  - `docs/plans/sync-v2-server/plan.md` `## Outcomes` "Layer→type mapping
    integrity and client-FK closure" paragraph reflects the corrected
    partition with an explicit cross-reference to the Deviations-log
    entry for t2 (PR #72).
  - `docs/plans/sync-v2-server/status.md` exists with all nine
    iterations, reverse-chronological (or as the as-built file has it),
    no gaps between iter 1 and iter 9.
- No code changes. Only files under `docs/plans/sync-v2-server/` are
  touched (plus, if commit `21c28f2` is included, `tasks/t4.md` — but
  that card is doc-only and t4 already shipped).
- `./scripts/quality-slow.sh backend` and `./scripts/quality-fast.sh`
  both still exit 0 after the merge. (Documentation-only change cannot
  affect gate output; this is a sanity check, not the load-bearing
  assertion.)

**Output artifact:**

- `docs/plans/sync-v2-server/plan.md` — `## Deviations log` populated;
  `## Outcomes` layer partition corrected.
- `docs/plans/sync-v2-server/status.md` — new file, nine iterations.
- (Optional) `docs/plans/sync-v2-server/tasks/t4.md` — corrected layer
  partition if commit `21c28f2` is in the merge set.

**Out of scope:**

- Any code change. Migrations, RPC bodies, contract tests, drift
  checker, tFINAL E2E scripts — all untouched. None of the failures the
  audit surfaced are code-level.
- The downstream `docs/plans/sync-v2-client/` and
  `docs/plans/sync-v2-launch/` stub plans. Their `## Inputs` already
  reference this plan by path; once the deviations log on `main` is
  populated, the client/launch coordinators will read it directly.
- Re-running the audit. The FAIL → PASS transition only needs
  re-verifying the three documentation gaps; the eleven code-level
  outcome assertions have already passed on `main` HEAD `df5e97f`.
