# Operations Docs

Process / workflow docs that complement the product/tech specs in `docs/specs/**`.

## Index

- `task-execution.md` — Plan / task / orchestrator protocol for executing large changes via parallel PRs dispatched by a coordinator session. Opt-in; see `docs/plans/README.md` for the trigger.

## Boundary

- Operations docs own _how we work_.
- Product/domain rules live in `docs/specs/**`.
- Local worktree contract lives in `docs/specs/12-worktree-config-and-isolation.md`.
- Runtime operator notes live in `RUNBOOK.md`.
