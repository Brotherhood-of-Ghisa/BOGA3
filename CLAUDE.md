# CLAUDE.md

The session entrypoint is **`AGENTS.md`** — read it first.

This file exists only so the `CLAUDE.md` filename also resolves to the same
guidance; it intentionally duplicates nothing. All authoritative rules live in
**`AGENTS.md`** and **`docs/specs/**`**.
