# CLAUDE.md

The session entrypoint is **`AGENTS.md`** — read it first. This file exists only so
the `CLAUDE.md` name also resolves; it carries **no rules of its own**.

All authoritative guidance lives in **`AGENTS.md`** and **`docs/specs/**`** — the
testing policy (including "never skip a runnable test" and the per-change
slow-gate triggers), the multi-agent-orchestration conventions, environment /
bootstrap, and everything else. Do not duplicate any of it here; update
`AGENTS.md` (or the relevant spec) instead.
