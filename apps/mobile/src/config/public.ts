/**
 * Public, non-secret defaults embedded in the mobile bundle.
 *
 * This URL is intentionally a setup page, not an OAuth consent URL. OAuth
 * request state is created by the MCP client and must never be added here.
 */
export const DEFAULT_BOGA_AGENT_CONNECT_URL =
  'https://sparkling-violet-dc56.sboschianpest.workers.dev/connect';
