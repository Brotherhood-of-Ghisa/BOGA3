APP_ENV=local
APP_NAME=BOGA-backend

# `./boga worktree start` links `supabase/functions/.env.local` to
# `~/.config/boga/edge-functions/env.shared` for shared machine-level function
# identity defaults.
